{% extends './base_front.html.twig' %}

{% block title %}Virement{% endblock %}

{% block body %}
</br>
</br>
</br>
</br>
</br>
</br>
    <h1>Virement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID Virement</th>
                <td>{{ virement.idVirement }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ virement.montant }}</td>
            </tr>
            <tr>
                <th>Date Virement</th>
                <td>{{ virement.dateVirement ? virement.dateVirement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ virement.titre }}</td>
            </tr>
            <tr>
                <th>Prenom Benefeciaire</th>
                <td>{{ virement.prenomBenef }}</td>
            </tr>
            <tr>
                <th>Rib Benefeciaire</th>
                <td>{{ virement.ribBenef }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_virement_index') }}">Back to list</a>
    
    <a href="{{ path('app_virement_edit', {'id_virement': virement.idVirement}) }}">edit</a>
    
    <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a class="btn btn-danger" href="{{ asset('/virement/mypdf') }}">Download PDF</a></li>
                    </ol>
                </nav>

    {{ include('virement/_delete_form.html.twig') }}
{% endblock %}
