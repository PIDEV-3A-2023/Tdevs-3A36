<form method="post" action="{{ path('app_virement_delete', {'id_virement': virement.idVirement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ virement.idVirement) }}">
    <button class="btn btn-danger">Delete</button>
</form>
